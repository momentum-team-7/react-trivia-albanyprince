// export default function Answer ({ question, handleAnswer }) {
    // const [answer, setAnswer] = useState ([])

    // useEffect(() => {
    //    axios.get(`https://opentdb.com/api.php?amount=10&category=${category.id}&type=multiple`).then((response) => {setAnswer(response.get)}


